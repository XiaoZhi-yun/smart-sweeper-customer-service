# 智扫通机器人智能客服 - 项目完整复盘

> 本文档面向学习者，从零开始逐层拆解整个项目的架构、代码实现和功能案例，帮助你彻底吃透这个项目。

---

## 目录

- [一、项目定位与技术栈](#一项目定位与技术栈)
- [二、项目目录结构](#二项目目录结构)
- [三、核心架构总览](#三核心架构总览)
- [四、从启动到运行：一次完整请求的生命周期](#四从启动到运行一次完整请求的生命周期)
- [五、核心模块逐个击破](#五核心模块逐个击破)
  - [5.1 入口层：app.py](#51-入口层apppy)
  - [5.2 Agent 核心层：react_agent.py](#52-agent-核心层react_agentpy)
  - [5.3 工具层：agent_tools.py](#53-工具层agent_toolspy)
  - [5.4 中间件层：middleware.py](#54-中间件层middlewarepy)
  - [5.5 RAG 服务层：rag_service.py](#55-rag-服务层rag_servicepy)
  - [5.6 向量库层：vector_store.py](#56-向量库层vector_storepy)
  - [5.7 模型工厂层：factory.py](#57-模型工厂层factorypy)
  - [5.8 工具层：utils/](#58-工具层utils)
- [六、功能案例全流程解析](#六功能案例全流程解析)
  - [案例 1：基础问答 - "扫地机器人怎么清理毛发？"](#案例-1基础问答---扫地机器人怎么清理毛发)
  - [案例 2：环境问答 - "深圳今天天气怎么样？适合扫地吗？"](#案例-2环境问答---深圳今天天气怎么样适合扫地吗)
  - [案例 3：报告生成 - "生成我的使用报告"](#案例-3报告生成---生成我的使用报告)
  - [案例 4：多轮对话 + 历史压缩](#案例-4多轮对话--历史压缩)
- [七、关键技术点深度解析](#七关键技术点深度解析)
- [八、启动与调试指南](#八启动与调试指南)
- [九、扩展与优化方向](#九扩展与优化方向)
- [十、核心知识点总结](#十核心知识点总结)
- [十一、项目迭代变更记录](#十一项目迭代变更记录)
- [十二、代码阅读路线图](#十二代码阅读路线图)
  - [12.1 总览](#121-总览)
  - [12.2 各阶段阅读要点](#122-各阶段阅读要点)
  - [12.3 配套阅读建议](#123-配套阅读建议)
  - [12.4 调试技巧速查](#124-调试技巧速查)

---

## 一、项目定位与技术栈

### 1.1 项目是什么

**智扫通** 是一个基于 **ReAct（Reason + Act）范式**的智能客服系统，专门为**扫地机器人/扫拖一体机器人**的用户提供服务。

### 1.2 核心能力一览

| 能力 | 说明 | 实现方式 |
|------|------|----------|
| 🤖 智能对话 | 回答扫地机器人相关的各类问题 | ReAct Agent 自主推理 |
| 📚 知识检索 | 从专业知识库中检索资料辅助回答 | RAG 检索增强生成 |
| 🌤️ 环境分析 | 结合天气、湿度等环境因素给出使用建议 | 天气 API + 用户定位 |
| 📊 报告生成 | 为用户生成个性化的使用情况报告 | 专用提示词 + CSV 数据查询 |

### 1.3 技术栈

| 类别 | 技术 | 版本 | 作用 |
|------|------|------|------|
| LLM 框架 | LangChain | 1.3.14 | 构建 Agent、工具、中间件 |
| LLM 运行时 | LangGraph | 1.2.10 | 状态管理、流式执行、上下文共享 |
| 大模型 | 阿里通义 qwen3-max | - | 对话生成、推理 |
| 嵌入模型 | text-embedding-v4 | - | 文本转向量 |
| 向量库 | Chroma | 1.5.9 | 存储文档向量、相似度检索 |
| 前端 | Streamlit | 1.60.0 | 快速搭建 Web UI |
| 文档处理 | PyPDFLoader / TextLoader | - | 加载 PDF/TXT 文件 |

### 1.4 架构设计理念

- **ReAct 模式**：先推理再行动，循环调用工具直至得出答案
- **RAG 技术**：检索增强生成，结合向量库检索私有知识
- **工厂模式**：统一接口生产不同类型的模型
- **单例模式**：确保 VectorStoreService 等资源密集型服务全局唯一
- **中间件机制**：在 Agent 执行过程中织入监控、日志、动态提示词切换
- **流式输出**：将模型生成内容逐字/逐段返回前端，实现"打字机"效果

---

## 二、项目目录结构

```
AgentProject/
├── app.py                              # 🏠 入口文件：Streamlit Web UI
│
├── agent/                              # 🤖 Agent 核心层
│   ├── react_agent.py                  #   ReAct Agent 主类
│   └── tools/
│       ├── agent_tools.py              #   Agent 可调用的工具集合
│       └── middleware.py               #   中间件（日志、监控、提示词切换）
│
├── rag/                                # 📚 RAG 检索增强层
│   ├── vector_store.py                 #   向量库服务（Chroma 封装）
│   └── rag_service.py                  #   RAG 总结服务
│
├── model/                              # 🧠 模型工厂层
│   └── factory.py                      #   统一创建聊天/嵌入模型
│
├── utils/                              # 🔧 工具层
│   ├── logger_handler.py               #   日志工具（控制台+文件双输出）
│   ├── config_handler.py               #   YAML 配置加载
│   ├── prompt_loader.py                #   提示词文件加载
│   ├── file_handler.py                 #   文件处理（MD5、加载器）
│   └── path_tool.py                    #   路径工具
│
├── config/                             # ⚙️ 配置文件（YAML）
│   ├── rag.yml                         #   模型配置（模型名称）
│   ├── chroma.yml                      #   向量库配置（集合、分片参数）
│   ├── prompts.yml                     #   提示词路径配置
│   └── agent.yml                       #   Agent 配置（外部数据路径）
│
├── prompts/                            # 📝 提示词文件（TXT）
│   ├── main_prompt.txt                 #   系统提示词（人设+规则+工具说明）
│   ├── rag_summarize.txt               #   RAG 提示词（如何结合参考资料回答）
│   └── report_prompt.txt               #   报告生成提示词（如何写使用报告）
│
├── data/                               # 📂 知识库数据
│   ├── 选购指南.txt
│   ├── 维护保养.txt
│   ├── 扫地机器人100问.txt
│   ├── 扫拖一体机器人100问.txt
│   ├── 故障排除.txt
│   └── external/
│       └── records.csv                 #   用户使用记录（10个用户 × 12个月 = 120条）
│
├── requirement.txt                     # 📦 依赖清单
└── 运行.txt                            # 🚀 启动说明
```

---

## 三、核心架构总览

### 3.1 整体架构图

```
┌─────────────────────────────────────────────────────────────────────┐
│                         用户浏览器                                  │
│                                                                     │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │              Streamlit Web UI (app.py)                        │  │
│  │                                                               │  │
│  │  • 会话历史管理 (session_state)                              │  │
│  │  • 打字机效果渲染 (write_stream)                             │  │
│  │  • 报告关键词检测 (REPORT_KEYWORDS)                          │  │
│  │  • 加载动画 (st.spinner)                                     │  │
│  └───────────────────────────────────────────────────────────────┘  │
│                          │ 用户输入                                  │
└──────────────────────────┼──────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────────┐
│               ReactAgent (agent/react_agent.py)                     │
│                                                                     │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │                    ReAct 循环（状态机）                       │  │
│  │                                                               │  │
│  │   ┌─────────┐    ┌─────────┐    ┌─────────┐                   │  │
│  │   │ Reason  │───▶│  Act    │───▶│ Observe │───┐               │  │
│  │   │(推理)   │    │(调工具) │    │(看结果) │   │               │  │
│  │   └─────────┘    └─────────┘    └─────────┘   │               │  │
│  │        ▲                                         │               │  │
│  │        └─────────────────────────────────────────┘               │  │
│  │                    循环直到得出最终答案                         │  │
│  └───────────────────────────────────────────────────────────────┘  │
│                                                                     │
│  ┌───────────────────────────────────────────────────────────────┐  │
│  │                     中间件链（按顺序执行）                    │  │
│  │                                                               │  │
│  │  ① log_before_model     模型前置：日志 + Token 统计            │  │
│  │  ② report_prompt_switch 动态提示词：切换 system prompt         │  │
│  │  ③ monitor_tool         工具包裹：监控 + 报告模式触发          │  │
│  └───────────────────────────────────────────────────────────────┘  │
│                                                                     │
│  核心方法：execute_stream(query, history, is_report)                │
│  核心方法：_maybe_compress(messages) → 历史压缩                    │
└──────────────────────────┬──────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────────┐
│               工具集合 (agent/tools/agent_tools.py)                 │
│                                                                     │
│  工具名称              功能              真实数据源                │
│  ─────────────────────────────────────────────────────────────    │
│  rag_summarize    RAG 知识检索       Chroma 向量库               │
│  get_weather      天气查询           模拟数据                    │
│  get_user_location 用户定位          模拟数据（随机城市）         │
│  get_user_id      用户身份获取       模拟数据（随机 ID）          │
│  get_current_month 当前月份获取      系统时间                    │
│  fetch_external_data 使用记录查询     CSV 文件                   │
│  fill_context_for_report 报告模式触发  哨兵工具（间接通信）       │
└──────────────────────────┬──────────────────────────────────────────┘
                           │
          ┌────────────────┼────────────────┐
          ▼                ▼                ▼
┌──────────────┐  ┌──────────────┐  ┌──────────────┐
│  RAG 服务    │  │  天气/用户   │  │  外部数据    │
│ (rag_service)│  │  (模拟数据)  │  │  (CSV 读取)  │
└──────┬───────┘  └──────────────┘  └──────────────┘
       │
       ▼
┌──────────────┐
│  向量库      │
│ (Chroma)     │
└──────────────┘
```

### 3.2 数据流向全景

```
用户提问
   │
   ▼
┌─────────────────────────────────────────────────────────────────┐
│  app.py                                                          │
│  • 关键词检测 → is_report                                        │
│  • 历史消息构建 → conversation_history                           │
│  • 调用 execute_stream(query, history, is_report)                │
└──────────────────────────┬──────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│  ReactAgent.execute_stream()                                     │
│  • 消息组装：[历史...] + {role:"user", content:query}             │
│  • 历史压缩：超过 12 条时用模型摘要旧消息                         │
│  • 传入 context_data = {"report": is_report}                     │
│  • self.agent.stream(input_data, stream_mode="values", context)  │
└──────────────────────────┬──────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│  LangGraph Agent 执行循环                                        │
│                                                                   │
│  Step 1: before_model 中间件                                     │
│  ├── log_before_model: 记录消息数、Token 消耗                    │
│  └── report_prompt_switch: 动态切换系统提示词                    │
│                           │                                       │
│  Step 2: 模型推理                                                │
│  ├── 输入：messages + system_prompt                              │
│  ├── 输出：工具调用决策（如 "调用 rag_summarize"）或直接回答      │
│  └── 若是工具调用 → 进入 Step 3                                  │
│      若是最终回答 → stream 产出 → 循环结束                        │
│                           │                                       │
│  Step 3: wrap_tool_call 中间件                                    │
│  ├── monitor_tool: 记录工具名、参数、设置 report 标记            │
│  └── 执行实际工具函数                                            │
│                           │                                       │
│  Step 4: 工具执行结果返回                                        │
│  └── 回到 Step 1 继续循环                                        │
└──────────────────────────┬──────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│  app.py 接收流式输出                                             │
│  • capture() 逐字符 yield → 打字机效果                           │
│  • response_messages 缓存完整回答                                │
│  • 存入 st.session_state["message"]                              │
│  • st.rerun() 刷新页面                                          │
└─────────────────────────────────────────────────────────────────┘
```

---

## 四、从启动到运行：一次完整请求的生命周期

以用户在浏览器输入 **"扫地机器人怎么清理毛发？"** 为例，追踪代码的完整执行路径：

```
【浏览器】用户在 chat_input 输入框键入问题 → 回车发送
   │
   ▼
【app.py:35】 if prompt: 条件触发
   │
   ▼
【app.py:38-39】 渲染用户消息 + 追加到历史
   st.chat_message("user").write(prompt)
   st.session_state["message"].append({"role": "user", "content": "扫地机器人怎么清理毛发？"})
   │
   ▼
【app.py:43】 is_report = any(kw in prompt for kw in REPORT_KEYWORDS)
   → "扫地机器人怎么清理毛发？" 不含报告关键词 → is_report = False
   │
   ▼
【app.py:48】 conversation_history = list(st.session_state["message"][:-1])
   → 取除最后一条（当前用户消息）之外的所有历史消息
   │
   ▼
【app.py:53-57】 调用 Agent
   res_stream = agent.execute_stream(
       query="扫地机器人怎么清理毛发？",
       history=conversation_history,
       is_report=False,
   )
   │
   ▼
【react_agent.py:86-87】 消息组装
   messages = list(history or [])  → 历史消息
   messages.append({"role": "user", "content": "扫地机器人怎么清理毛发？"})
   │
   ▼
【react_agent.py:90】 历史压缩检查
   len(messages) <= 12 → 不压缩，直接返回
   │
   ▼
【react_agent.py:94-95】 组装输入
   input_data = {"messages": messages}
   context_data = {"report": False}
   │
   ▼
【react_agent.py:102】 开始流式执行
   for step in self.agent.stream(input_data, stream_mode="values", context=context_data):
       │
       ▼
   ┌─────── LangGraph 内部循环 ───────┐
   │                                    │
   │  Step A: before_model 中间件       │
   │  ├── log_before_model:             │
   │  │   logger.info("即将调用模型，带 N 条消息")  │
   │  └── report_prompt_switch:         │
   │      → context["report"] = False   │
   │      → 返回 load_system_prompts()  │
   │        (main_prompt.txt 内容)      │
   │                                    │
   │  Step B: 模型推理                  │
   │  模型收到 system prompt + messages │
   │  思考过程（隐式）：                │
   │  "用户问清理毛发的方法，            │
   │   这是专业知识，                   │
   │   我需要调用 rag_summarize 工具    │
   │   来获取专业知识"                  │
   │  → 决策：调用 rag_summarize        │
   │                                    │
   │  Step C: wrap_tool_call 中间件     │
   │  ├── monitor_tool:                 │
   │  │   logger.info("执行工具：rag_summarize")  │
   │  │   logger.info("传入参数：{query: '扫地机器人清理毛发方法'}")  │
   │  └── 调用实际工具函数              │
   │                                    │
   │  Step D: rag_summarize 工具执行    │
   │  ├── rag_service.rag_summarize()  │
   │  │   ├── retriever_docs(query)    │
   │  │   │   → Chroma 向量检索        │
   │  │   │   → 返回 top-3 相关文档    │
   │  │   ├── 拼装 context 字符串       │
   │  │   └── chain.invoke({input, context}) │
   │  │       → 模型结合参考资料生成回答 │
   │  └── 返回回答文本给 Agent          │
   │                                    │
   │  Step E: 工具结果返回              │
   │  模型再次推理：                    │
   │  "我已经得到了足够的信息，          │
   │   可以直接回答用户了"              │
   │  → 生成最终回答（不再调工具）      │
   │                                    │
   └────────────────────────────────────┘
   │
   ▼
【react_agent.py:104-111】 产出流式内容
   for step in stream:
       step_msgs = step.get("messages", [])
       latest_message = step_msgs[-1]
       if latest_message.content:
           yield latest_message.content.strip() + "\n"
   │
   ▼
【app.py:62-67】 capture 函数逐字符产出
   for chunk in res_stream:    # 每个 chunk 是一段文本
       response_messages.append(chunk)   # 缓存完整回答
       for char in chunk:       # 逐字符
           time.sleep(0.01)    # 10ms 延迟
           yield char           # 打字机效果
   │
   ▼
【app.py:71】 st.chat_message("assistant").write_stream(capture(...))
   → Streamlit 实时渲染打字机效果
   │
   ▼
【app.py:75-76】 缓存完整回答 + 存入历史
   full_response = "".join(response_messages)
   st.session_state["message"].append({"role": "assistant", "content": full_response})
   │
   ▼
【app.py:80】 st.rerun()
   → 页面刷新，历史消息重新渲染
```

---

## 五、核心模块逐个击破

### 5.1 入口层：app.py

**文件路径**：`app.py`

**职责**：Streamlit Web UI 入口，负责用户交互和 Agent 调用。

#### 关键代码解析

```python
# 第 7 行：报告关键词列表
REPORT_KEYWORDS = ["报告", "使用记录", "使用情况", "统计", "报表", "查一下我的", "生成我的"]
```
这是报告模式的**三级检测**的兜底方案。当模型没有正确调用 `fill_context_for_report` 工具时，通过用户输入中是否包含这些关键词来判断是否进入报告模式。

```python
# 第 16-23 行：session_state 初始化
if "agent" not in st.session_state:
    st.session_state["agent"] = ReactAgent()
if "message" not in st.session_state:
    st.session_state["message"] = []
```
`session_state` 是 Streamlit 的会话级存储，类似前端的 localStorage。每个用户浏览器有独立的 session_state。Agent 实例只创建一次，后续复用；消息历史跨页面刷新保留。

```python
# 第 53-57 行：调用 Agent 流式执行
res_stream = st.session_state["agent"].execute_stream(
    query=prompt,
    history=conversation_history,
    is_report=is_report,
)
```
这是连接前端和后端的**关键枢纽**。`execute_stream` 返回一个生成器，产出模型的流式回答。

```python
# 第 62-67 行：capture 函数 - 打字机效果
def capture(generator, cache_list):
    for chunk in generator:
        cache_list.append(chunk)       # 缓存完整回答
        for char in chunk:
            time.sleep(0.01)           # 10ms 延迟
            yield char                 # 逐字符产出
```
`capture` 是一个生成器函数，它做了两件事：1）逐字符 yield 实现打字机效果；2）把所有 chunk 追加到 cache_list 中，最后拼接成完整回答。

```python
# 第 71 行：write_stream 渲染
st.chat_message("assistant").write_stream(capture(res_stream, response_messages))
```
`write_stream` 需要接收一个**可迭代对象**。`capture()` 返回的是生成器，生成器是可迭代的，所以直接传入即可。

#### 执行流程图

```
用户输入 → 关键词检测 → 构建历史 → 调用 Agent
                                    │
               ┌────────────────────┘
               ▼
        流式生成器 res_stream
               │
               ▼
        capture 函数（逐字符产出 + 缓存）
               │
               ▼
        write_stream 渲染（打字机效果）
               │
               ▼
        拼接完整回答 → 存入历史 → rerun
```

---

### 5.2 Agent 核心层：react_agent.py

**文件路径**：`agent/react_agent.py`

**职责**：定义 ReAct Agent 类，负责初始化 Agent 并执行流式查询。

#### 关键类与方法

**`_AgentInput` 和 `_AgentContext`（TypedDict）**

```python
class _AgentInput(TypedDict):
    messages: list[dict[str, str]]

class _AgentContext(TypedDict, total=False):
    report: bool
```
使用 `TypedDict` 定义输入和上下文类型，而不是普通 `dict`，可以获得更好的类型提示和 IDE 检查。`total=False` 表示所有字段都是可选的。

**`ReactAgent.__init__`（初始化）**

```python
def __init__(self):
    self.model = chat_model
    self.agent = create_agent(
        model=chat_model,
        system_prompt=load_system_prompts(),
        tools=[7 个工具],
        middleware=[3 个中间件],
    )
```
`create_agent` 是 LangChain 新版统一创建 Agent 的工厂函数。它内部构建了一个 ReAct 状态机循环：模型推理 → 工具调用 → 结果观察 → 再次推理，直到模型认为可以直接回答而不需要工具时结束。

**`ReactAgent.execute_stream`（核心执行方法）**

```python
def execute_stream(self, query, history=None, is_report=False):
    # 1. 构建消息列表
    messages = list(history or [])
    messages.append({"role": "user", "content": query})
    
    # 2. 历史压缩
    messages = self._maybe_compress(messages)
    
    # 3. 组装输入和上下文
    input_data = {"messages": messages}
    context_data = {"report": is_report}
    
    # 4. 流式执行
    for step in self.agent.stream(input_data, stream_mode="values", context=context_data):
        step_msgs = step.get("messages", [])
        if not step_msgs:
            continue
        latest_message = step_msgs[-1]
        if latest_message.content:
            yield latest_message.content.strip() + "\n"
```
这是整个系统的**核心方法**。它做了以下事情：
1. 把历史消息和当前 query 组装成完整的消息列表
2. 调用 `_maybe_compress` 进行历史压缩
3. 通过 `stream_mode="values"` 流式执行 Agent，每一步产出完整的消息快照
4. 取每一步最后一条消息的内容作为流式输出

**`ReactAgent._maybe_compress`（历史压缩）**

```python
def _maybe_compress(self, messages, threshold=12, keep_recent=8):
    if len(messages) <= threshold:
        return messages
    
    old = messages[:-keep_recent]      # 旧消息
    recent = messages[-keep_recent:]   # 最近消息
    
    # 调用模型生成摘要
    transcript = "\n".join(f"{m['role']}: {m['content']}" for m in old)
    summary = self.model.invoke(f"请总结：\n{transcript}").content
    
    return [{"role": "user", "content": f"【历史摘要】{summary}"}] + recent
```
当对话消息超过 12 条时触发压缩。策略是：把旧消息（除最近 8 条外）用模型生成一段摘要，用摘要替代旧消息，保留最近的 8 条原样。这样既减少了 token 消耗，又保留了对话上下文。

---

### 5.3 工具层：agent_tools.py

**文件路径**：`agent/tools/agent_tools.py`

**职责**：定义 Agent 可以调用的所有工具函数。

#### @tool 装饰器说明

```python
@tool(description="从向量存储中检索参考资料，返回专业知识内容")
def rag_summarize(query: str) -> str:
    ...
```
`@tool` 装饰器的作用：
1. 把普通 Python 函数转换为 LangChain Tool 对象
2. `description` 参数是给模型看的"工具说明书"，模型据此决定何时调用此工具
3. 函数签名的类型注解（如 `query: str`）会自动成为工具的参数定义

#### 七大工具详解

| # | 工具名 | 入参 | 返回值 | 数据来源 | 使用场景 |
|---|--------|------|--------|----------|----------|
| 1 | `rag_summarize` | query (str) | 专业知识文本 | Chroma 向量库 | 需要专业知识回答时 |
| 2 | `get_weather` | city (str) | 天气详情字符串 | 模拟数据 | 判断环境是否适合使用 |
| 3 | `get_user_location` | 无 | 城市名字符串 | 模拟数据 | 需要知道用户位置时 |
| 4 | `get_user_id` | 无 | 用户 ID 字符串 | 模拟数据 | 需要用户身份时 |
| 5 | `get_current_month` | 无 | YYYY-MM 格式月份 | 系统时间 | 生成报告需要月份 |
| 6 | `fetch_external_data` | user_id, month | 使用记录字符串 | CSV 文件 | 生成报告查询数据 |
| 7 | `fill_context_for_report` | 无 | 触发报告模式的信号 | 哨兵工具 | 进入报告模式的开关 |

#### 关键工具代码解析

**`fetch_external_data` - 外部数据查询**

```python
@tool(description="根据用户ID和月份查询外部使用记录")
def fetch_external_data(user_id: str, month: str) -> str:
    _load_external_data()  # 懒加载 CSV 数据
    
    try:
        record = external_data[user_id][month]
        return "，".join([f"{k}：{v}" for k, v in record.items()])
    except KeyError:
        return ""
```
`_load_external_data` 使用**懒加载**策略：只在第一次调用 `fetch_external_data` 时才读取 CSV 文件到内存，之后所有查询都走内存缓存，避免重复文件 IO。CSV 数据结构是三层嵌套字典：`{用户ID: {月份: {指标字典}}}`。

**`fill_context_for_report` - 哨兵工具**

```python
@tool(description="无入参，调用后触发中间件切换到报告生成模式")
def fill_context_for_report():
    return "已切换至报告生成模式"
```
这是一个特殊的"哨兵工具"（Sentinel Tool）。它本身不做任何实际事情，返回值也只是一个固定字符串。它的真正作用是作为一个**信号**：当模型判断用户想生成报告时，会主动调用这个工具，`monitor_tool` 中间件监听到这个调用后，会把 `runtime.context["report"]` 置为 `True`，从而触发提示词切换。这是一种**间接通信**的设计模式。

---

### 5.4 中间件层：middleware.py

**文件路径**：`agent/tools/middleware.py`

**职责**：在 Agent 执行过程中织入监控、日志、动态提示词切换等逻辑。

#### 三种中间件装饰器

LangChain 提供了三种中间件装饰器，分别在 Agent 执行的不同阶段触发：

| 装饰器 | 触发时机 | 用途 |
|--------|----------|------|
| `@before_model` | 每次模型调用前 | 日志、Token 统计、上下文注入 |
| `@wrap_tool_call` | 每次工具调用时 | 监控、参数验证、报告模式触发 |
| `@dynamic_prompt` | 每次模型调用前 | 动态切换 system prompt |

#### 关键中间件解析

**`monitor_tool` - 工具监控中间件**

```python
@wrap_tool_call
def monitor_tool(request, handler):
    logger.info(f"执行工具：{request.tool_call['name']}")
    logger.info(f"传入参数：{request.tool_call['args']}")
    
    result = handler(request)  # 执行真正的工具函数
    
    # 哨兵检测：如果调用了 fill_context_for_report，设置 report 标记
    if request.tool_call['name'] == "fill_context_for_report":
        request.runtime.context["report"] = True
    
    return result
```
这是一个 `@wrap_tool_call` 中间件，类似 Web 框架的请求拦截器。它包裹了工具的调用过程：在工具执行前记录日志，执行后检查是否需要设置报告模式标记。核心逻辑是第 45-46 行的哨兵检测。

**`log_before_model` - 模型前置中间件**

```python
@before_model
def log_before_model(state, runtime):
    msg_count = len(state['messages'])
    logger.info(f"即将调用模型，带 {msg_count} 条消息")
    
    last_msg = state['messages'][-1]
    logger.debug(f"{type(last_msg).__name__} | {last_msg.content.strip()}")
    
    # Token 消耗统计
    if hasattr(last_msg, 'usage_metadata') and last_msg.usage_metadata:
        usage = last_msg.usage_metadata
        logger.info(f"Token 消耗 - 输入: {usage.get('input_tokens', 0)}, 输出: {usage.get('output_tokens', 0)}")
```
这是一个 `@before_model` 中间件，在每次大模型调用前执行。它记录了当前消息数量、最后一条消息内容，以及上一轮的 Token 消耗。Token 统计有助于监控成本和性能。

**`report_prompt_switch` - 动态提示词中间件**

```python
@dynamic_prompt
def report_prompt_switch(request):
    # 一级检测：runtime.context 中的 report 标记
    is_report = request.runtime.context.get("report", False)
    
    # 二级检测：关键词回退
    if not is_report:
        # 遍历消息找最近的 user 消息
        for msg in reversed(request.messages):
            if hasattr(msg, 'role') and msg.role == 'user':
                if any(kw in msg.content for kw in REPORT_KEYWORDS):
                    is_report = True
                break
    
    # 返回不同的系统提示词
    if is_report:
        return load_report_prompts()
    return load_system_prompts()
```
这是一个 `@dynamic_prompt` 中间件，在每次模型调用前动态决定使用哪个 system prompt。它实现了**三级报告意图检测**机制：

| 级别 | 检测方式 | 来源 | 可靠性 |
|------|---------|------|--------|
| 一级 | `context["report"]` 标记 | app.py 传入 / monitor_tool 设置 | ⭐⭐⭐ 最可靠 |
| 二级 | 关键词匹配 | 用户消息内容 | ⭐ 兜底方案 |

---

### 5.5 RAG 服务层：rag_service.py

**文件路径**：`rag/rag_service.py`

**职责**：RAG（检索增强生成）总结服务，负责检索文档并生成回答。

#### RAG 工作流程

```
用户提问
   │
   ▼
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│  向量检索   │ ──▶ │  上下文拼装 │ ──▶ │  模型生成   │
└─────────────┘     └─────────────┘     └─────────────┘
       │                                        │
       ▼                                        ▼
┌─────────────┐                          ┌─────────────┐
│  Chroma     │                          │  结合上下文 │
│  向量库     │                          │  生成回答   │
└─────────────┘                          └─────────────┘
```

#### 关键方法解析

**`RagSummarizeService.rag_summarize`**

```python
def rag_summarize(self, query: str) -> str:
    # Step 1: 向量检索
    context_docs = self.retriever_docs(query)
    
    # Step 2: 拼装上下文
    context = ""
    for i, doc in enumerate(context_docs, 1):
        context += f"【参考资料{i}】: 内容：{doc.page_content} | 元数据：{doc.metadata}\n"
    
    # Step 3: 调用链生成回答
    return self.chain.invoke({
        "input": query,
        "context": context,
    })
```
这是 RAG 的主流程，分三步：
1. **向量检索**：把用户问题转为向量，在 Chroma 向量库中找最相似的 top-k 个文档
2. **拼装上下文**：把检索到的文档格式化为带编号的参考资料字符串
3. **生成回答**：把用户问题和上下文一起交给模型，模型结合参考资料生成答案

**LCEL 链组装**

```python
def _init_chain(self):
    chain = self.prompt_template | RunnableLambda(log_prompt) | self.model | StrOutputParser()
    return chain
```
使用 LangChain Expression Language（LCEL）的 `|` 管道符组合组件：
1. `prompt_template`：把用户问题和上下文填入模板
2. `log_prompt`：打印 prompt 用于调试
3. `model`：调用大模型
4. `StrOutputParser`：把 AIMessage 解析为纯字符串

---

### 5.6 向量库层：vector_store.py

**文件路径**：`rag/vector_store.py`

**职责**：向量库服务，负责加载文档、文本分片、存储和检索向量。

#### 关键流程

**文档加载流程**

```
文件扫描 → MD5 去重 → 读取内容 → 文本分片 → 写入向量库 → 记录 MD5
```

**MD5 去重机制**

```python
@staticmethod
def _check_md5_hex(md5_for_check: str) -> bool:
    md5_store_path = get_abs_path(chroma_conf["md5_hex_store"])
    if not os.path.exists(md5_store_path):
        open(md5_store_path, "w").close()
        return False
    with open(md5_store_path, "r") as f:
        for line in f.readlines():
            if line.strip() == md5_for_check:
                return True
    return False
```
每个文件加载前会计算 MD5，检查 `md5.text` 记录文件中是否已存在。如果已存在则跳过，避免重复加载相同内容的文件。这保证了只有新增或修改过的文件才会被加载。

**单例模式**

```python
_vector_store_instance = None

def get_vector_store() -> VectorStoreService:
    global _vector_store_instance
    if _vector_store_instance is None:
        _vector_store_instance = VectorStoreService()
    return _vector_store_instance
```
使用**懒加载单例模式**：不在模块加载时立即创建，而是延迟到第一次调用时才创建。这样做的好处是：如果程序没有用到向量库（如只测试其他模块），可以节省启动时间。

**文本分片**

```python
self.spliter = RecursiveCharacterTextSplitter(
    chunk_size=200,       # 每段最大 200 字符
    chunk_overlap=20,     # 相邻段重叠 20 字符
    separators=["\n\n", "\n", ".", "!", "?", "。", "！", "？", " ", ""],
)
```
递归分片器按优先级逐级尝试切分：先按段落（`\n\n`），段落太长则按行（`\n`），行太长则按句号，以此类推。`chunk_overlap` 保证相邻分片有 20 字符的重叠，防止语义在切分边界处被截断。

---

### 5.7 模型工厂层：factory.py

**文件路径**：`model/factory.py`

**职责**：使用工厂模式统一创建聊天模型和嵌入模型。

#### 工厂模式结构

```python
class BaseModelFactory(ABC):
    @abstractmethod
    def generator(self): pass

class ChatModelFactory(BaseModelFactory):
    def generator(self):
        return ChatTongyi(model=rag_conf["chat_model_name"])

class EmbeddingsFactory(BaseModelFactory):
    def generator(self):
        return DashScopeEmbeddings(model=rag_conf["embedding_model_name"])
```

**工厂模式的好处**：
- 符合**开闭原则**：新增模型类型只需添加新的工厂子类
- 切换供应商简单：从阿里切到百度只需修改工厂类
- 统一接口：所有模型通过 `generator()` 方法获取

#### 模块级单例

```python
chat_model = ChatModelFactory().generator()
embed_model = EmbeddingsFactory().generator()
```
在模块加载时就创建好，其他模块通过 `from model.factory import chat_model, embed_model` 直接使用。这是一种简单的单例实现方式，适合资源密集型对象。

---

### 5.8 工具层：utils/

#### `path_tool.py` - 路径工具

```python
def get_project_root() -> str:
    current_file = os.path.abspath(__file__)      # utils/path_tool.py
    current_dir = os.path.dirname(current_file)   # utils/
    project_root = os.path.dirname(current_dir)   # AgentProject/
    return project_root

def get_abs_path(relative_path: str) -> str:
    return os.path.join(get_project_root(), relative_path)
```
通过 `__file__` 推算项目根目录，确保无论从哪里启动都能正确定位文件。

#### `config_handler.py` - 配置加载

```python
def _load_yaml(config_path, encoding="utf-8"):
    with open(config_path, "r", encoding=encoding) as f:
        return yaml.safe_load(f)

# 模块加载时立即读取所有配置
rag_conf = load_rag_config()
chroma_conf = load_chroma_config()
prompts_conf = load_prompts_config()
agent_conf = load_agent_config()
```
使用 `yaml.safe_load` 而非 `load`，更安全，防止恶意代码执行。配置文件只在程序启动时读取一次。

#### `prompt_loader.py` - 提示词加载

```python
def _read_prompt_by_key(key, error_prefix):
    prompt_path = get_abs_path(prompts_conf[key])
    with open(prompt_path, "r", encoding="utf-8") as f:
        return f.read()

def load_system_prompts():
    return _read_prompt_by_key("main_prompt_path", "load_system_prompts")

def load_rag_prompts():
    return _read_prompt_by_key("rag_summarize_prompt_path", "load_rag_prompts")

def load_report_prompts():
    return _read_prompt_by_key("report_prompt_path", "load_report_prompts")
```
三个加载函数共用一个通用的 `_read_prompt_by_key` 方法，消除了重复代码。

#### `file_handler.py` - 文件处理

```python
def get_file_md5_hex(filepath):
    md5_obj = hashlib.md5()
    with open(filepath, "rb") as f:
        while chunk := f.read(4096):  # 海象运算符
            md5_obj.update(chunk)
    return md5_obj.hexdigest()
```
使用海象运算符（`:=`）边读边赋值，分片读取防止大文件爆内存。

#### `logger_handler.py` - 日志工具

```python
def get_logger(name="agent", console_level=INFO, file_level=DEBUG):
    log = logging.getLogger(name)
    log.setLevel(logging.DEBUG)
    
    # 控制台 Handler
    console_handler = logging.StreamHandler()
    console_handler.setLevel(console_level)
    log.addHandler(console_handler)
    
    # 文件 Handler（按日期命名）
    log_file = os.path.join(LOG_ROOT, f"{name}_{datetime.now().strftime('%Y%m%d')}.log")
    file_handler = logging.FileHandler(log_file, encoding='utf-8')
    file_handler.setLevel(file_level)
    log.addHandler(file_handler)
    
    return log
```
同时输出到控制台和文件，控制台默认 INFO 级别，文件默认 DEBUG 级别，便于开发调试和问题排查。

---

## 六、功能案例全流程解析

### 案例 1：基础问答 - "扫地机器人怎么清理毛发？"

**用户视角**：在对话框输入问题，等待回答。

#### 完整执行流程

```
Step 1: 用户输入 "扫地机器人怎么清理毛发？"
        │
        ▼
Step 2: app.py 检测
        • is_report = False（无报告关键词）
        • 构建历史消息 + 当前 query
        • 调用 agent.execute_stream(query, history, is_report=False)
        │
        ▼
Step 3: ReactAgent 组装消息
        • messages = [历史..., {role:"user", content:"扫地机器人怎么清理毛发？"}]
        • _maybe_compress: 消息数 <= 12，不压缩
        • context_data = {"report": False}
        │
        ▼
Step 4: ReAct 循环（第一次）
        │
        ├── 4a. before_model 中间件
        │   • log_before_model: "即将调用模型，带 N 条消息"
        │   • report_prompt_switch: context["report"]=False
        │   → 返回 main_prompt.txt（系统提示词）
        │
        ├── 4b. 模型推理
        │   • 思考："用户问清理毛发，这是专业知识"
        │   • 决策：调用 rag_summarize(query="扫地机器人清理毛发方法")
        │
        ├── 4c. wrap_tool_call 中间件
        │   • monitor_tool: "执行工具：rag_summarize"
        │   • 执行 rag_summarize
        │
        ├── 4d. RAG 服务执行
        │   ├── retriever_docs(query)
        │   │   → Chroma 向量检索
        │   │   → 找到 3 条相关文档
        │   ├── 拼装 context 字符串
        │   │   "【参考资料1】: 内容：毛发清理92%..."
        │   │   "【参考资料2】: 内容：胶刷更换建议..."
        │   │   "【参考资料3】: 内容：尘盒清理频率..."
        │   └── chain.invoke({input, context})
        │       → 模型生成总结回答
        │
        ├── 4e. 模型第二次推理
        │   • 思考："已得到足够信息，可以直接回答"
        │   • 决策：生成最终回答（不再调工具）
        │
        └── 4f. 流式产出
            yield "根据参考资料，清理毛发的方法包括：\n"
            yield "1. 使用胶刷主刷，毛发不易缠绕\n"
            yield "2. 定期清理尘盒，建议每 3 天一次\n"
            yield "3. 短毛地毯建议开启地毯增压模式\n"
        │
        ▼
Step 5: app.py 接收流式输出
        • capture 函数逐字符 yield
        • write_stream 渲染打字机效果
        • 拼接完整回答存入历史
```

---

### 案例 2：环境问答 - "深圳今天天气怎么样？适合扫地吗？"

#### 完整执行流程

```
Step 1: 用户输入 → is_report = False
        │
        ▼
Step 2: ReAct 循环（第一次）
        • 模型推理："用户问天气，需要知道用户位置"
        • 决策：调用 get_user_location()
        │
        ▼
Step 3: 执行 get_user_location()
        • 随机返回："深圳"
        │
        ▼
Step 4: ReAct 循环（第二次）
        • 模型推理："知道用户在深圳了，查询深圳天气"
        • 决策：调用 get_weather(city="深圳")
        │
        ▼
Step 5: 执行 get_weather(city="深圳")
        • 返回："城市深圳天气为晴天，气温26摄氏度，空气湿度50%..."
        │
        ▼
Step 6: ReAct 循环（第三次）
        • 模型推理："天气晴朗，温度适宜，湿度适中，非常适合扫地"
        • 决策：直接回答
        │
        ▼
Step 7: 生成回答
        "深圳今天晴天，气温26°C，湿度50%，非常适合使用扫地机器人进行清扫。"
```

---

### 案例 3：报告生成 - "生成我的使用报告"

**这是最复杂的场景，涉及三级报告检测和专用提示词切换。**

#### 完整执行流程

```
Step 1: 用户输入 "生成我的使用报告"
        │
        ▼
Step 2: app.py 预处理
        • 关键词检测："生成"、"我的"、"报告" 命中 REPORT_KEYWORDS
        • is_report = True
        │
        ▼
Step 3: ReactAgent 组装
        • context_data = {"report": True}
        │
        ▼
Step 4: report_prompt_switch 中间件（一级检测）
        • 读取 context["report"] = True
        • 返回 report_prompt.txt（报告专用提示词）
        • 模型以"报告写手"角色开始思考
        │
        ▼
Step 5: 模型推理（报告模式）
        • 思考："要生成报告，需要用户ID、月份、使用记录"
        • 按报告生成强约束流程：
          ① 调用 fill_context_for_report()  ← 必调哨兵工具
          ② 调用 get_user_id()
          ③ 调用 get_current_month()
          ④ 调用 fetch_external_data(user_id, month)
        │
        ▼
Step 6: 工具调用链执行
        │
        ├── 6a. fill_context_for_report()
        │   • 返回："已切换至报告生成模式"
        │   • monitor_tool 检测到这个调用
        │   • 设置 context["report"] = True（双保险）
        │
        ├── 6b. get_user_id()
        │   • 随机返回："1001"
        │
        ├── 6c. get_current_month()
        │   • 返回系统当前年月，如 "2026-08"
        │
        ├── 6d. fetch_external_data(user_id="1001", month="2026-08")
        │   • _load_external_data() 懒加载 CSV（首次调用时）
        │   • 查找 external_data["1001"]["2026-08"]
        │   • 返回："特征：65㎡公寓 | 单身 | 木地板，效率：覆盖率:85%..."
        │
        ▼
Step 7: 模型生成报告
        • 使用 report_prompt.txt 模板
        • 标题：黑马程序员扫地机器人使用情况报告与保养建议
        • 内容：Markdown 格式的专业报告
        │
        ▼
Step 8: 流式输出到前端
```

#### 报告片段示例

```markdown
# 黑马程序员扫地机器人使用情况报告与保养建议

## 用户特征分析
- 户型：65㎡公寓
- 居住：单身
- 地面：木地板

## 清洁效率
- 覆盖率：85%
- 日均清扫：45㎡
- 漏扫区域：沙发底部（高度不足）

## 耗材状态
- 主刷寿命：剩余60天（建议提前备货）
- HEPA滤网：剩余40%（建议3个月内更换）

## 保养建议
1. 垫高沙发底部以解决漏扫问题
2. 定期清理主刷缠绕的毛发
3. 建议1个月内更换 HEPA 滤网
```

---

### 案例 4：多轮对话 + 历史压缩

#### 完整流程

```
Step 1: 用户第 1 轮 "我家是木地板，用什么扫地机器人好？"
        • Agent 调用 rag_summarize("木地板 扫地机器人推荐")
        • 返回：木地板机型推荐
        • 消息数：3 条（user + assistant + tool）

Step 2: 用户第 2 轮 "那如果我养宠物呢？"
        • Agent 理解上下文（木地板 + 养宠物）
        • 调用 rag_summarize("木地板 宠物 扫地机器人")
        • 返回：适合养宠家庭的木地板机型
        • 消息数：6 条

... 继续对话 ...

Step 8: 用户第 8 轮 "顺便问下需要注意什么？"
        • 此时消息数达到 15 条，超过阈值 12
        • 触发历史压缩：
          - 旧消息（15-8=7 条）→ 模型生成摘要
          - 最近 8 条保留原样
          - 压缩后：1（摘要）+ 8（最近）= 9 条

Step 9: Agent 基于压缩后的历史继续对话
        • 摘要消息以 "【历史摘要】" 开头
        • 模型能理解摘要内容，保持对话连贯性
```

---

## 七、关键技术点深度解析

### 7.1 ReAct 模式原理

ReAct = **Reason（推理）+ Act（行动）**

```
传统 LLM：  用户 → 模型 → 回答（一次性，无法获取外部信息）

ReAct Agent：用户 → 推理 → 调工具 → 观察结果 → 再推理 → ... → 回答
                                        │
                                        ▼
                                  循环直到得出答案
```

**优势**：
- 可以获取外部信息（天气、数据库、知识库）
- 可以进行多步推理和工具组合
- 可以处理超出模型训练数据的问题

### 7.2 RAG 检索增强生成

```
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│  用户提问   │ ──▶ │  向量检索   │ ──▶ │  模型生成   │
└─────────────┘     └─────────────┘     └─────────────┘
                           │                    │
                           ▼                    ▼
                    ┌─────────────┐     ┌─────────────┐
                    │  向量库     │     │  结合上下文 │
                    │  (Chroma)   │     │  生成回答   │
                    └─────────────┘     └─────────────┘
```

**关键步骤**：
1. **文本转向量**：使用 `text-embedding-v4` 模型把文档内容转成向量
2. **向量存储**：存入 Chroma 向量数据库（本地 sqlite）
3. **相似度检索**：用户提问转向量，找到最相似的 top-k 文档
4. **上下文拼装**：把检索到的文档拼成提示词上下文
5. **生成回答**：把用户问题 + 上下文交给模型生成答案

### 7.3 中间件机制

中间件类似于 **Web 框架的拦截器**，在 Agent 执行过程中织入逻辑：

```
请求进入 │
    ▼
┌─ before_model ──────────────────────────────────────────┐
│  log_before_model: 记录消息数、Token 消耗               │
│  report_prompt_switch: 切换系统提示词                    │
└─────────────────────────────────────────────────────────┘
    ▼
┌─ 模型推理 ──────────────────────────────────────────────┐
│  大模型生成回答或工具调用决策                            │
└─────────────────────────────────────────────────────────┘
    ▼
┌─ wrap_tool_call ────────────────────────────────────────┐
│  monitor_tool: 记录工具调用、检测报告模式                │
└─────────────────────────────────────────────────────────┘
    ▼
工具执行 │
    ▼
返回结果或循环回到 before_model │
```

### 7.4 报告模式三级检测

| 级别 | 检测方式 | 来源 | 可靠性 |
|------|---------|------|--------|
| 一级 | `context["report"]` 标记 | app.py 传入 / monitor_tool 设置 | ⭐⭐⭐ |
| 二级 | 用户消息关键词匹配 | `REPORT_KEYWORDS` 列表 | ⭐ |

**设计意图**：一级是最可靠的方式（由 app.py 明确传入），二级是兜底方案（防止标记遗漏）。这种多层次检测确保报告模式不会因为某个环节的失误而遗漏。

### 7.5 工厂模式创建模型

```python
class BaseModelFactory(ABC):
    @abstractmethod
    def generator(self): ...

class ChatModelFactory(BaseModelFactory):
    def generator(self):
        return ChatTongyi(model=rag_conf["chat_model_name"])

class EmbeddingsFactory(BaseModelFactory):
    def generator(self):
        return DashScopeEmbeddings(model=rag_conf["embedding_model_name"])

# 使用
chat_model = ChatModelFactory().generator()   # qwen3-max
embed_model = EmbeddingsFactory().generator()  # text-embedding-v4
```

### 7.6 单例模式应用

**VectorStoreService 懒加载单例**：
```python
_vector_store_instance = None

def get_vector_store():
    global _vector_store_instance
    if _vector_store_instance is None:
        _vector_store_instance = VectorStoreService()
    return _vector_store_instance
```

**为什么用懒加载？**
- Chroma 初始化需要时间
- 不是所有功能都需要向量库
- 第一次用到时才创建，节省启动时间

### 7.7 历史压缩机制

```python
def _maybe_compress(self, messages, threshold=12, keep_recent=8):
    if len(messages) <= threshold:
        return messages
    
    old = messages[:-8]      # 旧消息
    recent = messages[-8:]   # 最近 8 条
    
    summary = self.model.invoke(f"请总结：\n{transcript}").content
    return [{"role": "user", "content": f"【历史摘要】{summary}"}] + recent
```

**压缩策略**：
- 超过 12 条消息时触发
- 旧消息做摘要，保留最近 8 条
- 摘要消息以 "【历史摘要】" 开头
- 压缩失败时退化为直接截断

**常见疑问：压缩后还能保持上下文联系吗？**

可以，但会有精度损失。压缩的本质是「用摘要替代旧消息」：

```
压缩前（13条）:                        压缩后（9条）:
  user: 我家是木地板                      user: 【历史摘要】用户家是木地板，
  assistant: 推荐XX型号                    问过宠物和价格，推荐了YY型号...
  user: 那养宠物呢？                      user: 那续航怎么样？
  assistant: 推荐YY型号                   assistant: YY续航45分钟
  user: 哪个便宜？                        ... (共9条)
  assistant: YY型号更便宜
  ... (共13条)
```

模型看到 `【历史摘要】` 会理解这是之前的对话摘要，能保持大方向的上下文。但**细节可能丢失**（比如具体的型号数字、精确的价格）。这是「用精度换 Token」的权衡。

**压缩过程对用户不可见**：压缩在 Agent 内部透明执行，`st.session_state["message"]` 始终保存完整的原始对话，用户界面不会显示摘要消息。可通过日志 `[历史压缩]` 关键词验证压缩是否触发。

### 7.8 流式输出原理

```python
# 生成器逐段产出
for step in self.agent.stream(input_data, stream_mode="values", context=context_data):
    step_msgs = step.get("messages", [])
    latest_message = step_msgs[-1]
    if latest_message.content:
        yield latest_message.content.strip() + "\n"

# capture 函数逐字符产出（打字机效果）
def capture(generator, cache_list):
    for chunk in generator:
        cache_list.append(chunk)
        for char in chunk:
            time.sleep(0.01)
            yield char

# Streamlit 接收生成器并渲染
st.chat_message("assistant").write_stream(capture(res_stream, response_messages))
```

---

## 八、启动与调试指南

### 8.1 启动项目

```bash
# 进入项目目录
cd D:\PycharmProjects\AgentProject

# 激活虚拟环境
# Windows PowerShell:
.\.venv\Scripts\Activate.ps1
# Windows CMD:
.\.venv\Scripts\activate.bat

# 运行 Streamlit 应用
streamlit run app.py
```

### 8.2 本地测试各模块

```bash
# 测试配置加载
python -m utils.config_handler

# 测试日志功能
python -m utils.logger_handler

# 测试路径工具
python -m utils.path_tool

# 测试提示词加载
python -m utils.prompt_loader

# 测试向量库（首次运行会加载所有文档）
python -m rag.vector_store

# 测试 RAG 服务
python -m rag.rag_service

# 测试 Agent（命令行模式）
python -m agent.react_agent
```

### 8.3 查看日志

日志文件保存在 `logs/` 目录，按日期命名：
```
logs/agent_20260801.log
```

**日志级别说明**：
- **INFO**：关键流程日志（工具调用、消息数量）
- **DEBUG**：详细调试日志（完整 prompt、Token 消耗）
- **ERROR**：错误日志（工具调用失败、文件读取失败）

### 8.4 部署到另一台电脑

```bash
# 1. 克隆/复制项目代码

# 2. 创建虚拟环境
python -m venv .venv

# 3. 激活虚拟环境
.\.venv\Scripts\Activate.ps1

# 4. 安装依赖
pip install -r requirement.txt

# 5. 配置阿里 DashScope API Key
#    方式一：设置环境变量
#    set DASHSCOPE_API_KEY=你的APIKey
#    方式二：在代码中通过环境变量读取

# 6. 启动项目
streamlit run app.py

# 7. 浏览器访问
#    http://localhost:8501
```

---

## 九、扩展与优化方向

### 9.1 当前可扩展点

| 模块 | 扩展方式 |
|------|---------|
| `agent_tools.py` | 添加新工具函数，用 `@tool` 装饰 |
| `data/` | 新增知识库文件（.txt/.pdf），自动加载到向量库 |
| `prompts/` | 修改提示词文件，调整 AI 行为 |
| `config/rag.yml` | 切换模型供应商或模型版本 |
| `records.csv` | 增加更多用户使用记录 |

### 9.2 可能的优化方向

| 方向 | 说明 |
|------|------|
| 用户鉴权 | 当前用随机 ID，实际应从登录态获取 |
| 天气 API | 对接真实天气 API（和风天气、高德） |
| 向量库重建 | 定期重建向量库，清理过期数据 |
| 缓存策略 | RAG 检索结果可加缓存，减少重复计算 |
| 并发处理 | 支持多用户并发访问 |
| 反馈学习 | 收集用户反馈，优化提示词和工具 |
| 流式细化 | 使用 `stream_mode="messages"` 实现 token 级流式 |

---

## 十、核心知识点总结

| 知识点 | 在项目中的应用位置 |
|--------|-------------------|
| **ReAct 模式** | `react_agent.py` - Agent 的核心推理循环 |
| **RAG 技术** | `rag_service.py` + `vector_store.py` - 检索增强生成 |
| **工厂模式** | `factory.py` - 统一接口生产不同类型的模型 |
| **单例模式** | `vector_store.py` - VectorStoreService 全局唯一 |
| **中间件** | `middleware.py` - 工具监控、日志、提示词切换 |
| **流式输出** | `react_agent.py` + `app.py` - 生成器 + 打字机效果 |
| **历史压缩** | `react_agent.py` - 长对话自动摘要 |
| **类型提示** | `react_agent.py` - TypedDict 定义输入/上下文 |
| **懒加载** | `vector_store.py` + `agent_tools.py` - 首次调用才加载 |
| **哨兵模式** | `agent_tools.py` - `fill_context_for_report` 间接通信 |
| **LCEL 管道** | `rag_service.py` - `prompt \| model \| parser` 链式组合 |
| **MD5 去重** | `vector_store.py` - 避免知识库重复加载 |
| **会话状态** | `app.py` - session_state 跨轮次保留对话 |

---

## 十一、项目迭代变更记录

> 本章节记录项目从初期到现在完成的所有更改，包括 Bug 修复、功能增强、架构优化和代码质量提升。

### 11.1 Bug 修复（5 项）

| # | 问题 | 原代码问题 | 修复方式 |
|---|------|-----------|---------|
| 1 | `get_current_month` 返回错误时间 | 返回随机月份而非系统当前月份 | 改用 `datetime.now().strftime("%Y-%m")` 获取真实系统时间 |
| 2 | `execute_stream` 丢失对话历史 | 未接收 `history` 参数，多轮对话上下文丢失 | 新增 `history` 参数，将 `st.session_state["message"]` 中的历史消息传入 Agent |
| 3 | 流式输出空响应崩溃 | `response_messages[-1]` 在空列表时抛 `IndexError` | 改用 `"".join(response_messages)` 拼接，空列表时给兜底文案 |
| 4 | `write_stream` 报类型错误 | 用 `lambda:` 包裹生成器，Streamlit 无法迭代 lambda | 直接传入 `capture()` 返回的生成器对象 |
| 5 | Token 消耗统计不生效 | 两个问题：① 只检查 `last_msg` 取不到 AIMessage（因为最后一条常是 HumanMessage 或 ToolMessage）；② 阿里通义把 Token 放在 `response_metadata['token_usage']` 而非 `usage_metadata` | ① 改为向前遍历找最近的 AIMessage；② 优先取 `usage_metadata`，回退取 `response_metadata['token_usage']` |

### 11.2 功能增强（5 项）

| # | 功能 | 说明 |
|---|------|------|
| 1 | **多轮对话支持** | `execute_stream` 新增 `history` 参数，通过 `st.session_state["message"]` 传递完整对话历史 |
| 2 | **历史压缩机制** | 消息超过 12 条时，用模型摘要旧消息（保留最近 8 条），防止超出 Token 限制 |
| 3 | **三级报告意图检测** | 一级：`context["report"]` 标记；二级：`fill_context_for_report` 工具调用；三级：用户消息关键词匹配。确保报告模式不会遗漏 |
| 4 | **Token 消耗统计** | `log_before_model` 中间件从 `usage_metadata` 提取输入/输出 Token 消耗，记录到日志 |
| 5 | **异常兜底** | `execute_stream` 外层 `try/except` 捕获所有异常，返回友好错误信息而非崩溃 |

### 11.3 架构优化（4 项）

| # | 优化 | 说明 |
|---|------|------|
| 1 | **VectorStoreService 单例** | 全局只创建一个 Chroma 连接，避免重复初始化浪费资源。使用懒加载策略，首次调用才创建 |
| 2 | **TypedDict 类型定义** | 用 `_AgentInput` 和 `_AgentContext` 两个 `TypedDict` 替代裸 `dict`，获得 IDE 类型提示和检查 |
| 3 | **CSV 数据懒加载** | `_load_external_data()` 只在首次调用 `fetch_external_data` 时读取 CSV 文件到内存，后续查询走缓存 |
| 4 | **MD5 去重逻辑完善** | 文档加载时计算 MD5 并与 `md5.text` 记录对比，只加载新增或修改过的文件 |

### 11.4 代码质量提升（3 项）

| # | 改进 | 说明 |
|---|------|------|
| 1 | **逐行中文注释** | 为所有 `.py` 文件补充简洁易懂的中文注释，重点在核心逻辑处添加行内注释 |
| 2 | **消除重复代码** | `prompt_loader.py` 中三个加载函数共用 `_read_prompt_by_key` 通用方法 |
| 3 | **IDE 警告修复** | 修复了类型不匹配、变量遮蔽等 IDE 警告（如 `agent/react_agent.py` 中 `input_dict` 和 `context` 的类型警告） |

### 11.5 新增文件（2 项）

| 文件 | 说明 |
|------|------|
| `项目复盘.md` | 完整的项目复盘文档，包含架构图、案例解析、技术点精讲 |
| `requirement.txt` | 项目依赖清单，方便在另一台电脑上部署 |

### 11.6 修改前后对比一览

```
修改前                              修改后
──────────────────────────────────────────────────────────────
get_current_month: 返回随机月份      → 返回真实系统时间
execute_stream: 无 history 参数     → 支持多轮对话历史传递
历史消息: 无压缩                    → 超 12 条自动摘要压缩
报告检测: 单一方式（依赖工具调用）  → 三级检测（标记+工具+关键词）
VectorStore: 每次创建新实例         → 全局单例 + 懒加载
外部数据: 每次读文件                → 懒加载 + 内存缓存
流式输出: 空响应崩溃                → 兜底文案
异常处理: 无                        → try/except 友好兜底
Token统计: 取不到数据               → 遍历找AIMessage + 兼容阿里通义元数据
代码注释: 较少                      → 逐行中文注释
```

总计完成了 **5 项 Bug 修复、5 项功能增强、4 项架构优化、3 项代码质量提升、2 个新文件创建**。

---

## 十二、代码阅读路线图

> 本章节为学习者提供从入门到精通的代码阅读顺序，按照**依赖关系 + 认知规律**，从底层到上层、从简单到复杂，循序渐进。

### 12.1 总览

```
阶段 1：基础配置层（5 分钟）
  ┌─────────────────────────────────────────────┐
  │  ① config/rag.yml                          │  ← 项目的"户口本"
  │  ② config/chroma.yml                       │  ← 向量库的"户口本"
  │  ③ config/prompts.yml                      │  ← 提示词的"户口本"
  └─────────────────────────────────────────────┘
         │ 理解：项目需要什么配置、配置长什么样
         ▼
阶段 2：工具层（15 分钟）
  ┌─────────────────────────────────────────────┐
  │  ④ utils/path_tool.py                      │  ← 最简单，热身用
  │  ⑤ utils/config_handler.py                 │  ← 看看配置怎么加载进来的
  │  ⑥ utils/logger_handler.py                 │  ← 日志是怎么建的
  │  ⑦ utils/file_handler.py                   │  ← MD5 怎么算的
  │  ⑧ utils/prompt_loader.py                 │  ← 提示词怎么读的
  └─────────────────────────────────────────────┘
         │ 理解：工具函数怎么写的，配置怎么读的
         ▼
阶段 3：模型层（10 分钟）
  ┌─────────────────────────────────────────────┐
  │  ⑨ model/factory.py                        │  ← 工厂模式怎么生产模型
  └─────────────────────────────────────────────┘
         │ 理解：模型是怎么创建的，工厂模式怎么用的
         ▼
阶段 4：RAG 层（20 分钟）
  ┌─────────────────────────────────────────────┐
  │  ⑩ rag/vector_store.py                     │  ← 向量库怎么建的
  │  ⑪ rag/rag_service.py                      │  ← RAG 怎么问答的
  └─────────────────────────────────────────────┘
         │ 理解：文档怎么变向量，怎么检索，怎么生成回答
         ▼
阶段 5：Agent 层（30 分钟）
  ┌─────────────────────────────────────────────┐
  │  ⑫ agent/tools/agent_tools.py              │  ← Agent 有哪些武器
  │  ⑬ agent/tools/middleware.py               │  ← Agent 的"守卫"
  │  ⑭ agent/react_agent.py                    │  ← Agent 大脑
  └─────────────────────────────────────────────┘
         │ 理解：Agent 怎么思考、怎么调工具、中间件怎么织入
         ▼
阶段 6：应用层（10 分钟）
  ┌─────────────────────────────────────────────┐
  │  ⑮ app.py                                  │  ← 最终的 Web 界面
  └─────────────────────────────────────────────┘
         │ 理解：所有组件怎么串起来，最终怎么跑起来
```

### 12.2 各阶段阅读要点

#### 阶段 1：配置文件 — 知道"是什么"

| 顺序 | 文件 | 重点关注 |
|------|------|---------|
| ① | `config/rag.yml` | 只有两行：聊天模型名 + 嵌入模型名 |
| ② | `config/chroma.yml` | 向量库路径、文档路径、MD5 存储路径 |
| ③ | `config/prompts.yml` | 三个提示词文件的路径 |

> 💡 **看完你应该知道**：项目用到了哪些资源，它们在哪里。

#### 阶段 2：工具层 — 知道"怎么读配置"

| 顺序 | 文件 | 重点关注 |
|------|------|---------|
| ④ | `utils/path_tool.py` | `get_project_root()` → 从 `__file__` 推导出项目根目录 |
| ⑤ | `utils/config_handler.py` | `load_rag_config()` 等函数 → 从 yml 文件读配置，模块加载时就存入变量 |
| ⑥ | `utils/logger_handler.py` | `get_logger()` → 两个 Handler（控制台 + 文件），日志级别策略 |
| ⑦ | `utils/file_handler.py` | `get_file_md5_hex()` → 分块读取文件计算 MD5 |
| ⑧ | `utils/prompt_loader.py` | `_read_prompt_by_key()` → 三个加载函数共用的通用方法 |

> 💡 **看完你应该知道**：配置怎么进来的、日志怎么打的、文件怎么处理的。这些是基础设施，所有业务代码都会依赖它们。

#### 阶段 3：模型层 — 知道"模型怎么来的"

| 顺序 | 文件 | 重点关注 |
|------|------|---------|
| ⑨ | `model/factory.py` | 抽象基类 `BaseModelFactory` + 两个工厂子类 + 模块级单例 |

```
阅读提示：
  1. 先看 BaseModelFactory 抽象基类 → 理解工厂模式的骨架
  2. 再看 ChatModelFactory → 聊天模型怎么生产的
  3. 再看 EmbeddingsFactory → 嵌入模型怎么生产的
  4. 最后看末尾的 chat_model 和 embed_model → 单例怎么实现的
```

> 💡 **看完你应该知道**：工厂模式怎么用的，为什么用单例，两个模型有什么区别（一个生成文本、一个生成向量）。

#### 阶段 4：RAG 层 — 知道"知识怎么来的"

| 顺序 | 文件 | 重点关注 |
|------|------|---------|
| ⑩ | `rag/vector_store.py` | `VectorStoreService` 类：文档加载 → 分片 → 存向量 → 检索。重点看 `_check_md5_hex()` 去重和 `get_vector_store()` 单例 |
| ⑪ | `rag/rag_service.py` | `RagSummarizeService` 类：`rag_summarize()` 方法三步流程（检索 → 拼装 → 生成），以及 LCEL 链组装 |

```
阅读提示：
  vector_store.py（存储和检索）→ 是"图书馆"
  rag_service.py（调用和生成）→ 是"图书管理员"，帮你找书并总结内容
```

> 💡 **看完你应该知道**：RAG 的完整流程，文档怎么变向量存起来，用户提问时怎么找到相关文档并生成回答。

#### 阶段 5：Agent 层 — 知道"智能体怎么工作的"

| 顺序 | 文件 | 重点关注 |
|------|------|---------|
| ⑫ | `agent/tools/agent_tools.py` | 每个 `@tool` 装饰的函数 → Agent 能调用的能力。重点看 `fill_context_for_report` 哨兵工具的设计 |
| ⑬ | `agent/tools/middleware.py` | 三种中间件装饰器 → 分别在什么时候触发、做了什么 |
| ⑭ | `agent/react_agent.py` | 核心类 `ReactAgent`：`__init__` 组装所有组件、`execute_stream` 主流程、`_maybe_compress` 历史压缩 |

```
阅读提示：
  agent_tools.py → Agent 的"工具箱"，每个函数看入参和返回值即可
  middleware.py  → Agent 的"门卫"，在关键节点插日志和切换提示词
  react_agent.py → Agent 的"大脑"，把所有东西串起来
```

> 💡 **看完你应该知道**：Agent 怎么"思考"、怎么"决定"调用哪个工具、中间件怎么织入逻辑、历史压缩怎么做的。这是项目最核心的部分。

#### 阶段 6：应用层 — 知道"最终怎么跑起来"

| 顺序 | 文件 | 重点关注 |
|------|------|---------|
| ⑮ | `app.py` | Streamlit 页面搭建 → session_state 管理历史 → 调用 Agent → 流式渲染 |

```
阅读提示：
  1. 页面初始化（标题、session_state）
  2. 渲染历史消息
  3. 接收用户输入
  4. 关键词检测 → 构建历史 → 调用 Agent → 流式输出 → 存入历史
```

> 💡 **看完你应该知道**：所有组件怎么串成一个完整的 Web 应用。

### 12.3 配套阅读建议

| 辅助材料 | 用途 |
|---------|------|
| `项目复盘.md` 各章节 | 每读完一个阶段，回看文档中对应的章节加深理解 |
| `data/` 目录 | 对照 RAG 层阅读时，看看哪些文档会被加载 |
| `logs/` 目录 | 启动项目后，对照日志理解 Agent 的实际执行流程 |
| `prompts/` 目录 | 对照 Agent 层阅读时，看看系统提示词长什么样 |

### 12.4 调试技巧速查

```
1. 打开 DEBUG 日志查看完整 prompt
2. 在 middleware 中加断点观察 Agent 状态
3. 修改 prompts 文件快速调整 AI 行为
4. 修改 records.csv 测试不同用户的报告生成
5. 搜索日志关键词 [历史压缩] 验证压缩是否触发
6. 搜索日志关键词 [token usage] 查看 Token 消耗
```

---

> **最后寄语**：这个项目是一个典型的 **LangChain Agent + RAG** 应用，涵盖了智能体开发的核心技术栈。吃透这个项目后，你将具备独立开发类似 AI 应用的能力。建议动手修改代码、添加新工具、调整提示词，在实践中加深理解。